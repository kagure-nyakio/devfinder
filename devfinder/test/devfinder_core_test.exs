defmodule DevfinderCoreTest do
  use ExUnit.Case

  alias Devfinder.Core.Dev
  
  describe "find_dev/1" do
    test "successfully fetches developer's information given a username" 
    test "returns the expected response when user is not found" 
  end
end
